name: Publishing crate

on:
  push:
    branches:
      - main

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
          ssh-key: ${{ secrets.PULL_KEY_REPO }}

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: clippy, rustfmt
          target: wasm32-unknown-unknown
          toolchain: stable
          # Disable the action's built-in rust-cache so the explicit
          # Swatinem/rust-cache step below is the single cache owner.
          cache: false

      # Save/restore the cargo registry + git index, ~/.cargo/bin
      # (installed CLIs like cargo-release), and the target/ build
      # artifacts. shared-key lets the build and publish jobs reuse it.
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: crate-workflow
          cache-bin: true

      - name: Building packages
        env:
          RUSTFLAGS: "-D warnings"
        run: |
          make build

  publish:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
          ssh-key: ${{ secrets.PULL_KEY_REPO }}

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: clippy, rustfmt
          target: wasm32-unknown-unknown
          toolchain: stable
          # Disable the action's built-in rust-cache so the explicit
          # Swatinem/rust-cache step below is the single cache owner.
          cache: false

      # Same shared-key as the build job → restores its cargo registry,
      # target/ artifacts, and the cached cargo-release binary.
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: crate-workflow
          cache-bin: true

      - name: Install release
        run: |
          # --locked is a no-op recompile when the cached binary is
          # already present at the same version; otherwise installs it.
          cargo install cargo-release --locked

      - name: Publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          cargo release --unpublished --execute

  # docs-build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #       with:
  #         submodules: recursive
  #         ssh-key: ${{ secrets.PULL_KEY_REPO }}
  #     - uses: actions-rust-lang/setup-rust-toolchain@v1
  #       with:
  #         components: clippy, rustfmt
  #         target: wasm32-unknown-unknown
  #         toolchain: stable

  #     - name: Build static files
  #       run: |
  #         cargo install mdbook

  #     - name: Build static files
  #       run: |
  #         mdbook build

  #     - name: Upload static files as artifact
  #       id: deployment
  #       uses: actions/upload-pages-artifact@v3
  #       with:
  #         path: .build/docs/

  # deploy:
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   runs-on: ubuntu-latest
  #   needs: docs-build
  #   steps:
  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v4
